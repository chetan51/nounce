   Framework
.1   * NCNotificationManager
.1     ~ (NSMutableDictionary *)notifications

       + listen
           // Listens for events about a notification from Notification Center
           // and notifies the specified objects for that notification
           
.1     + notify:(NCNotification *)notification
.1         // Overwrites any existing notification
           
       + clear:(NCNotification *)notification
       
       + clearAll
       
       + expire:(NCNotification *)notification
       

   Application (Notification Center)
 1   * AppController
.1     - listen
.1           // Listen to messages from all other applications

.1     - notificationPosted:(NSNotification *)message
 
.1     - notify:(NCNotification *)notification
       
       - clearNotification:(NCNotification *)notification
       
.1     - UIShowNotificationPane

.1     - UIHideNotificationPane
       
       - UISortApplicationToTopOfNotificationPane:(NSApplication *)application
       
       - UIShowNotificationBubbleWithNotifications:(NSArray *)notifications
       
       - NCNotificationHovered:(id)sender
       
       - NCNotificationClicked:(id)sender
       
       - NCNotificationClearClicked:(id)sender
       
       - NCNotificationButtonClicked:(id)sender

 1   * NotificationPane (Javascript client)
 1     - notify (String notificationID, String notificationTitle, HTML notificationContent, String fromAppID, String fromAppName)
       
.1   * NCNotificationCenter (singleton)
.1     ~ (NSMutableDictionary *)applications
.1           // Indexed by application ID
.1     ~ (NSMutableDictionary *)notifications
.1           // Indexed by application ID
       ~ (NSMutableArray *)expiredNotifications
             // Ordered by time expired

.1     - notify:(NCNotification *)notification
.1         // Overwrites any existing notification
       
       - clear:(NCNotification *)notification
       
       - clearAllForApp:(NCApplication *)app
       
       - expire:(NCNotification *)notification
       
       - submitForm:(NCInputForm *)inputForm
           submitButtonID:(NSString *)id
       

   Shared Classes
.1   * NCNotification
.1     ~ (NSNumber *)id
.1     ~ (NCApplication *)fromApp
.1     ~ (NSString *)title
.1     ~ (NSString *)textContent
       ~ (NSString *)htmlContent
       ~ (NSString *)iconPath
       ~ (NSString *)htmlInput
       ~ (id)callbackObject
       ~ (selector)callbackSelector

.1     + notificationWithTitle:(NSString *)title
.1         textContent:(NSString *)textContent
           htmlContent:(NSString *)htmlContent (^)
           iconPath:(NSString *)iconPath (^)
           htmlInput:(NSString *)htmlInput (^)
           type:(NCNotificationType)type
           expires:(NSDate *)date
           
       - notifyOnEvent:(id)callbackObject
           withSelector:(selector)callbackSelector
             // The selector is called with (NCEvent *)event
         
       + textNotificationWithTitle:(NSString *)title
           textContent:(NSString *)textContent
           iconPath:(NSString *)iconPath (^)
           htmlInput:(NSString *)htmlInput (^)
           type:(NCNotificationType)type
           expires:(NSDate *)date
           
       + customNotificationWithTitle:(NSString *)title
           htmlContent:(NSString *)htmlContent (^)
           iconPath:(NSString *)iconPath (^)
           htmlInput:(NSString *)htmlInput (^)
           type:(NCNotificationType)type
           expires:(NSDate *)date
         
.1   * NCApplication
.1     ~ (NSString *)id
.1     ~ (NSString *)name
       ~ (NSString *)iconPath
.1     
.1     + applicationWithBundleIdentifier:(NSString *)bundleIdentifier

     * NCNotificationType
       # DONE
       # IN_NCOGRESS
       # TIME_SENSITIVE
       
     * NCEvent
       # READ
       # CLEARED
       # INPUT_SUBMIT