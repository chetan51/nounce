   Framework
.1   * NCNotificationManager
.1     ~ (NSMutableDictionary *)notifications

       + listen
           // Listens for events about a notification from Notification Center
           // and notifies the specified objects for that notification
           
.1     + notify:(NCNotification *)notification
.1         // Overwrites any existing notification
           
       + clear:(NCNotification *)notification
       
       + clearAll
       
       + expire:(NCNotification *)notification
       

   Application (Notification Center)
 1   * AppController
.1     - listen
.1           // Listen to messages from all other applications

.1     - notificationPosted:(NSNotification *)message
 
.1     - notify:(NCNotification *)notification
       
 1     - showNotificationPaneForApps:(NSArray *)apps
       
       - showNotificationPaneWithFirstApp:(NCApplication *)app
       
       - showNotificationBubbleWithNotifications:(NCApplication *)notifications
       
       - notificationHovered:(id)sender
       
       - notificationClicked:(id)sender
       
       - notificationClearClicked:(id)sender
       
       - notificationButtonClicked:(id)sender
       
       - clearNotification:(NCNotification *)notification
       
       - showInputForm:(NCInputForm *)inputForm
           forNotification:(NCNotification *)notification
             // Hides all other input forms for containing notification

 1   * NCNotificationPane
 1     - showWithApplicationViews:(NSArray *)applicationViews
       
       - showBubbleWithApplicationViews:(NSArray *)applicationViews
           forTime:(NSNumber *)milliseconds

       - hide
       
 1   * NCNotificationCenter (singleton)
.1     ~ (NSMutableDictionary *)applications
.1           // Indexed by application ID
.1     ~ (NSMutableDictionary *)notifications
.1           // Indexed by application ID
       ~ (NSMutableArray *)expiredNotifications
             // Ordered by time expired

.1     - notify:(NCNotification *)notification
.1         // Overwrites any existing notification
       
       - clear:(NCNotification *)notification
       
       - clearAllForApp:(NCApplication *)app
       
       - expire:(NCNotification *)notification
       
       - submitForm:(NCInputForm *)inputForm
           submitButtonID:(NSString *)id
       
 1   * NCViewController (singleton)
 1     ~ (NSArrayController *)applicationViews
      
 1     - notify:(NCNotification *)notification
     
.1   * NCApplicationView
.1     ~ (NCApplication *)application
.1     ~ (NSArrayController *)notificationViews

.1   * NCNotificationView
.1     ~ (NCNotification *)notification


   Shared Classes
.1   * NCNotification
.1     ~ (NSNumber *)id
.1     ~ (NCApplication *)fromApp
.1     ~ (NSString *)title
.1     ~ (NSString *)textContent
       ~ (NSView *)customContent
       ~ (NSImage *)icon
       ~ (NCInputForm *)inputForm
       ~ (id)callbackObject
       ~ (selector)callbackSelector

.1     + notificationWithTitle:(NSString *)title
.1         textContent:(NSString *)textContent
           customContent:(NSView *)customContent (^)
           icon:(NSImage *)icon (^)
           inputForm:(NCInputForm *)inputForm (^)
           type:(NCNotificationType)type
           expires:(NSDate *)date
           
       - notifyOnEvent:(id)callbackObject
           withSelector:(selector)callbackSelector
             // The selector is called with (NCEvent *)event
         
       + textNotificationWithTitle:(NSString *)title
           message:(NSString *)message
           icon:(NSImage *)icon (^)
           inputForm:(NCInputForm *)inputForm (^)
           type:(NCNotificationType)type
           expires:(NSDate *)date
           
       + customNotificationWithTitle:(NSString *)title
           content:(NSView *)content
           icon:(NSImage *)icon (^)
           inputForm:(NCInputForm *)inputForm (^)
           type:(NCNotificationType)type
           expires:(NSDate *)date
         
.1   * NCApplication
.1     ~ (NSString *)id
.1     ~ (NSString *)name
.1     
.1     + applicationWithBundleIdentifier:(NSString *)bundleIdentifier

     * NCNotificationType
       # DONE
       # IN_NCOGRESS
       # TIME_SENSITIVE
       
     * NCEvent
       # READ
       # CLEARED
       # INPUT_SUBMIT
       
     * NCButton
       ~ (NSString *)id
       ~ (NSButton *)button
       ~ (NCButtonType)type
       ~ (BOOL)hideFormOnClick

     * NCButtonType
       # SUBMIT
       # SHOW_FORM
       
     * NCInputForm
       ~ (NSMutableArray *)elements
       
       - notifyOnSubmit:(id)callbackObject
           withSelector:(selector)callbackSelector
             // The selector is called with (NSArray *)formData
        
       - addInputTextField:(NSTextField *)textField
           id:(NSString *)id

       - addSubmitButton:(NSButton *)button
           id:(NSString *)id
           hideFormOnClick:(BOOL)hideForm
       
       - addSubForm:(NCInputForm *)form
           showOnButtonClick:(NSButton *)button